{
  "url": "/blacklist",
  "method": "GET",
  "qs": {
    "confidenceMinimum": "{{parameters.confidenceMinimum}}"
  },
  "response": {
    "iterate": "{{body.data || body}}",
    "output": "{{item}}",
    "limit": "{{parameters.limit}}"
  }
}
