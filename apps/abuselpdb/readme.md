# AbuseIPDB Make Custom App

This generated app provides a minimal, local Make custom app for AbuseIPDB.

## API documentation

- Docs: https://docs.abuseipdb.com/
- Base URL: `https://api.abuseipdb.com/api/v2`
- Authentication: Key header

## Included module

- **Make an API Call** (`makeAnApiCall`) sends a custom request relative to the documented base URL and applies the app-specific authentication behavior.

## Notes

Do not add Key in module headers; the app sends it from the connection.
